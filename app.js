let pronom = ['El', 'Nuestro', 'Vuestro' ];

let adj = ['único', 'incomparable', 'maravilloso'];

let nomb = ['boss', 'nerd' ,'puto amo'];

let dom = ['.es','.hack','.apok','.com' ]

    for (let i = 0; i < pronom.length; i++) {
         
        for (let j = 0; j < adj.length; j++) {

        for (let k = 0; k < nomb.length; k++) {
    
        for (let l = 0; l < nomb.length; l++) {
        
    
     console.log(pronom[i]+adj[j]+nomb [k]+ dom[l]);
    
     }
    } 
  } 
}
